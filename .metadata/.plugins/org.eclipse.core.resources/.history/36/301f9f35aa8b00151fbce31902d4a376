<?php
// Задача 3:
// Да се състави програма, чрез която се въвеждат последователно две
// редици от символи без интервали.
// Програмата да извежда съобщение за резултата от сравнението им по
// позиции.
// Пример: хипопотам, хипопотук
// Изход:
// Двата низа са с равна дължина.
// Разлика по позиции:
// 8 а-у
// 9 м-к


$str1 = 'qwerty';
$str2 = 'qwerfg';


$len1 = mb_strlen($str1, 'UTF-8');
$len2 = mb_strlen($str2, 'UTF-8');

$flag = $len1 == $len2 ? 1 : 0;
$flagText = 0;
if ($flag) {
	echo 'there are same length' ,PHP_EOL;
	for ($i = 0; $i < $len1; $i++) {
		
		if ($str1[$i] != $str2[$i] && !$flagText) {
			$flagText = 1;
			echo 'Разлика по позиции:';
		}
		if ($str1[$i] != $str2[$i]) {
			echo "$i  $str1[$i]-$str2[$i]";
		}
	};
}

