<?php

function fiboNumbs($n) {
	
	
	$arr = [0, 1, 1];
	
	$len = count($arr);
	
	
	for ($i = 3; $i < $n; $i++) {
		
		$arr[$i] = $arr[$i - 1] + $arr[$i - 2];
		
	}
	
	return $arr[$len - 1];
	
}

var_dump(fiboNumbs(7));